let Koa,dotenv,mongoose;_bf7‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_bf7‍.w("dotnev",[["default",["dotenv"],function(v){dotenv=v}]]);_bf7‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



dotenv.config()

const { PORT, MONGO_URI } = process.env;

async function connectDB(){
    try{
        await mongoose.connect(MONGO_URI,{
            useNewUrlParser:true,
            useFindAndModify:false,
            useUnifiedTopology:true
        })
         console.log('Connected to MongoDB');
  } catch (e) {
    _bf7‍.g.console.error(e);
  }

}
connectDB()
const app = new Koa()

//response
app.use((ctx) => (ctx.body = 'hello koa'))

const port = PORT || 4000
app.listen(port,()=>{
    console.log(`port는 ${port}`)
})